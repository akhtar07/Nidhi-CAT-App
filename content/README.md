# /content

Versioned content shipped with the app: `syllabus.json`, lessons, questions,
passage sets, and generated JSON Schemas (`/content/schemas/`).

Content is data, not code — see SPEC.md §1.3. Populated starting Milestone 1.
